<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#F6F2EC" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="default" />
  <meta name="apple-mobile-web-app-title" content="第二地址" />
  <meta name="format-detection" content="telephone=no" />
  <title>第二地址 · Second Address</title>
  <style>
:root {
  --bg: #f6f2ec;
  --bg-2: #efe8df;
  --card: rgba(255, 255, 255, .74);
  --card-strong: rgba(255,255,255,.92);
  --ink: #262522;
  --muted: #77736c;
  --line: rgba(66, 59, 50, .10);
  --accent: #7e8e7f;
  --accent-2: #b99986;
  --soft-blue: #9aa9b8;
  --soft-gold: #b9a47e;
  --danger: #a66f6a;
  --shadow: 0 18px 45px rgba(60, 52, 44, .10);
  --radius: 24px;
  --safe-top: env(safe-area-inset-top, 0px);
  --safe-bottom: env(safe-area-inset-bottom, 0px);
  font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", "PingFang SC", "Noto Sans CJK SC", Arial, sans-serif;
  color-scheme: light;
}

* { box-sizing: border-box; }
html, body { margin: 0; min-height: 100%; background: var(--bg); color: var(--ink); }
body {
  min-height: 100vh;
  min-height: 100dvh;
  overflow-x: hidden;
  background:
    radial-gradient(circle at 8% 4%, rgba(185, 153, 134, .20), transparent 30%),
    radial-gradient(circle at 92% 14%, rgba(154, 169, 184, .20), transparent 31%),
    linear-gradient(180deg, #f8f5f0 0%, var(--bg) 46%, #f2ede7 100%);
}
body::before {
  content: "";
  position: fixed;
  inset: 0;
  pointer-events: none;
  opacity: .16;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 140 140' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.06'/%3E%3C/svg%3E");
  mix-blend-mode: multiply;
}
button, input, textarea, select { font: inherit; }
button { -webkit-tap-highlight-color: transparent; }

.app-shell { width: min(100%, 760px); min-height: 100dvh; margin: 0 auto; position: relative; }
.topbar {
  position: sticky; top: 0; z-index: 20;
  display: flex; justify-content: space-between; align-items: flex-end;
  padding: calc(18px + var(--safe-top)) 20px 12px;
  backdrop-filter: blur(18px) saturate(1.1);
  -webkit-backdrop-filter: blur(18px) saturate(1.1);
  background: linear-gradient(180deg, rgba(246,242,236,.92), rgba(246,242,236,.72), rgba(246,242,236,0));
}
.eyebrow { font-size: 10px; letter-spacing: .18em; color: var(--muted); font-weight: 700; }
h1 { margin: 4px 0 0; font-size: 31px; letter-spacing: -.04em; line-height: 1; }
.icon-btn {
  width: 42px; height: 42px; border-radius: 50%; border: 1px solid var(--line);
  background: rgba(255,255,255,.58); color: var(--ink); box-shadow: 0 8px 20px rgba(55,48,42,.06);
}
.main-content { padding: 8px 16px calc(110px + var(--safe-bottom)); }
.bottom-nav {
  position: fixed; bottom: 0; left: 50%; transform: translateX(-50%); z-index: 30;
  width: min(100%, 760px);
  display: grid; grid-template-columns: repeat(5, 1fr);
  padding: 10px 10px calc(8px + var(--safe-bottom));
  background: rgba(247,243,238,.88); border-top: 1px solid var(--line);
  backdrop-filter: blur(24px) saturate(1.2); -webkit-backdrop-filter: blur(24px) saturate(1.2);
}
.nav-item { border: 0; background: transparent; color: #8a857d; display: flex; flex-direction: column; align-items: center; gap: 3px; font-size: 10px; padding: 3px 0; }
.nav-item.active { color: var(--ink); }
.nav-icon { font-size: 21px; line-height: 1; font-weight: 500; }

.hero {
  position: relative; overflow: hidden; border-radius: 30px; padding: 24px 22px;
  min-height: 225px; box-shadow: var(--shadow);
  background:
    radial-gradient(circle at 82% 16%, rgba(255,255,255,.9), transparent 25%),
    linear-gradient(135deg, rgba(185,153,134,.48), rgba(154,169,184,.38) 55%, rgba(126,142,127,.43));
}
.hero::after { content: ""; position: absolute; right: -30px; bottom: -62px; width: 190px; height: 190px; border-radius: 50%; border: 1px solid rgba(255,255,255,.45); box-shadow: 0 0 0 28px rgba(255,255,255,.08), 0 0 0 58px rgba(255,255,255,.05); }
.hero-meta { position: relative; z-index: 2; display: flex; justify-content: space-between; gap: 12px; align-items: flex-start; }
.hero h2 { margin: 10px 0 8px; font-size: 34px; letter-spacing: -.04em; max-width: 70%; }
.hero p { margin: 0; color: rgba(38,37,34,.72); line-height: 1.55; max-width: 76%; }
.status-pill { background: rgba(255,255,255,.54); border: 1px solid rgba(255,255,255,.42); border-radius: 999px; padding: 7px 10px; font-size: 12px; white-space: nowrap; }
.hero-actions { position: relative; z-index: 2; display: flex; gap: 8px; margin-top: 24px; flex-wrap: wrap; }

.section { margin-top: 18px; }
.section-head { display: flex; justify-content: space-between; align-items: baseline; gap: 12px; padding: 0 4px; margin-bottom: 9px; }
.section-title { font-size: 14px; font-weight: 700; letter-spacing: -.01em; }
.section-note { font-size: 12px; color: var(--muted); }
.card {
  background: var(--card); border: 1px solid rgba(255,255,255,.75); border-radius: var(--radius);
  box-shadow: 0 12px 36px rgba(64,54,45,.06); padding: 16px;
  backdrop-filter: blur(16px); -webkit-backdrop-filter: blur(16px);
}
.card + .card { margin-top: 10px; }
.card.soft { background: rgba(255,255,255,.52); box-shadow: none; }
.card-title { font-weight: 700; margin: 0 0 6px; }
.card-copy { color: var(--muted); line-height: 1.55; font-size: 14px; }
.quick-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
.quick-card { min-height: 126px; display: flex; flex-direction: column; justify-content: space-between; cursor: pointer; }
.quick-card.full { grid-column: 1 / -1; min-height: 96px; }
.quick-mark { font-size: 24px; opacity: .72; }
.quick-label { font-size: 16px; font-weight: 700; }
.quick-sub { font-size: 12px; color: var(--muted); margin-top: 4px; }

.btn { border: 0; border-radius: 999px; padding: 11px 15px; font-weight: 700; background: rgba(255,255,255,.70); color: var(--ink); box-shadow: 0 7px 18px rgba(55,48,42,.06); }
.btn.primary { background: #292824; color: #fff; }
.btn.subtle { background: rgba(255,255,255,.48); border: 1px solid var(--line); box-shadow: none; }
.btn.danger { background: rgba(166,111,106,.12); color: #8b544f; }
.btn.block { width: 100%; }
.btn:active, .quick-card:active { transform: scale(.985); }

.chips { display: flex; flex-wrap: wrap; gap: 8px; }
.chip { border: 1px solid var(--line); background: rgba(255,255,255,.60); color: #615d57; border-radius: 999px; padding: 8px 11px; font-size: 13px; }
.chip.selected { background: #2d2c28; color: #fff; border-color: #2d2c28; }

.field { margin-top: 14px; }
.field:first-child { margin-top: 0; }
.label { display: block; font-size: 12px; color: var(--muted); margin: 0 0 7px 2px; }
.input, .textarea, .select {
  width: 100%; border: 1px solid rgba(58,51,44,.09); background: rgba(255,255,255,.70); color: var(--ink);
  border-radius: 18px; padding: 13px 14px; outline: none; box-shadow: inset 0 1px 0 rgba(255,255,255,.55);
}
.textarea { min-height: 104px; resize: vertical; line-height: 1.55; }
.input:focus, .textarea:focus, .select:focus { border-color: rgba(126,142,127,.55); box-shadow: 0 0 0 3px rgba(126,142,127,.10); }
.form-actions { display: flex; gap: 8px; margin-top: 16px; }
.form-actions .btn { flex: 1; }

.metric-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; }
.metric { background: rgba(255,255,255,.62); border: 1px solid rgba(255,255,255,.74); border-radius: 20px; padding: 14px; }
.metric-value { font-size: 26px; font-weight: 760; letter-spacing: -.04em; }
.metric-label { font-size: 11px; color: var(--muted); margin-top: 3px; }

.timeline { position: relative; }
.timeline-item { display: grid; grid-template-columns: 44px 1fr; gap: 10px; padding: 11px 0; }
.timeline-item + .timeline-item { border-top: 1px solid var(--line); }
.timeline-dot { width: 34px; height: 34px; border-radius: 50%; background: rgba(126,142,127,.13); display: grid; place-items: center; font-size: 13px; }
.timeline-title { font-weight: 700; font-size: 14px; }
.timeline-meta { color: var(--muted); font-size: 11px; margin-top: 3px; }
.timeline-copy { margin-top: 6px; color: #4e4b46; line-height: 1.48; font-size: 13px; white-space: pre-wrap; }

.bar-row { margin-top: 12px; }
.bar-head { display: flex; justify-content: space-between; font-size: 12px; color: var(--muted); margin-bottom: 5px; }
.bar { height: 9px; border-radius: 999px; background: rgba(50,46,42,.06); overflow: hidden; }
.bar > span { display: block; height: 100%; border-radius: inherit; background: linear-gradient(90deg, var(--accent), var(--soft-blue)); }

.empty { text-align: center; padding: 28px 20px; color: var(--muted); }
.empty .symbol { display: block; font-size: 34px; margin-bottom: 10px; opacity: .6; }

.callout { padding: 13px 14px; border-radius: 18px; background: rgba(126,142,127,.10); color: #555f56; font-size: 13px; line-height: 1.5; }
.callout.warn { background: rgba(185,164,126,.14); color: #71634a; }
.callout.danger { background: rgba(166,111,106,.12); color: #7e5752; }

.range-wrap { padding: 8px 3px 0; }
.range { width: 100%; accent-color: var(--accent); }
.range-labels { display: flex; justify-content: space-between; font-size: 10px; color: var(--muted); margin-top: 4px; }

.sheet { border: 0; padding: 0; background: transparent; width: 100%; height: 100%; max-width: none; max-height: none; margin: 0; }
.sheet::backdrop { background: rgba(34,31,28,.24); backdrop-filter: blur(5px); -webkit-backdrop-filter: blur(5px); }
.sheet[open] { display: flex; align-items: flex-end; justify-content: center; }
.sheet-card { width: min(100%, 760px); max-height: 88dvh; overflow: auto; background: #f8f5f0; border-radius: 30px 30px 0 0; padding: 9px 18px calc(24px + var(--safe-bottom)); box-shadow: 0 -20px 50px rgba(30,27,25,.18); }
.sheet-handle { width: 36px; height: 4px; border-radius: 999px; background: rgba(48,43,39,.18); margin: 2px auto 14px; }
.sheet-title { font-size: 24px; letter-spacing: -.03em; margin: 0 0 6px; }
.sheet-sub { margin: 0 0 18px; color: var(--muted); line-height: 1.5; font-size: 13px; }

.timer { text-align: center; padding: 20px 0 10px; }
.timer-number { font-size: 64px; letter-spacing: -.06em; font-weight: 760; 